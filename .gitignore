# 覆盖率报告
coverage.out
coverage.out.*

# 构建产物
/dist/
/bin/
/tmp/

# 示例落盘目录（go run ./examples/fidelity -dump ./out）
/out/

# 本地环境变量（切勿提交）
.env
.env.local

# macOS Finder 元数据
.DS_Store

# 编辑器
/.idea/
/.vscode/

# 本地 git worktree（改代码在此开，合回 main 后移除）
/.worktrees/

# Cursor 计划草稿（临时产物，不入库）；但 .cursor/rules/ 是跨 agent 规则，要跟踪
/.cursor/plans/
