# 覆盖率报告
coverage.out

# 构建产物
/dist/
/bin/
/tmp/

# 示例落盘目录（go run ./examples/fidelity -dump ./out）
/out/

# 本地环境变量（切勿提交）
.env
.env.local

# macOS Finder 元数据
.DS_Store

# 编辑器
/.idea/
/.vscode/

# Cursor 计划草稿（临时产物，不入库）；但 .cursor/rules/ 是跨 agent 规则，要跟踪
/.cursor/plans/
